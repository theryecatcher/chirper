{{define "title"}}Chirper{{end}}
{{define "head"}}{{end}}
{{define "content"}}
<div class="container">
	<div class="page-header">
		<h1>Hello, {{.name}}</h1>
	</div>
	<p>You have arrived. Click {{LINK "Tweets" "here"}} to view your tweets.</p>
	{{template "footer" .}}
</div>
{{end}}
{{define "foot"}}{{end}}